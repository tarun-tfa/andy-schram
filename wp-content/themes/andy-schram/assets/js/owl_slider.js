jQuery(document).ready(function ($) {

    $(".owl-founders-slider").owlCarousel({
        // center: true,
        items:3,
        loop:true,
        margin:10,
        nav:true,
        // dots:true
    });
    
   });